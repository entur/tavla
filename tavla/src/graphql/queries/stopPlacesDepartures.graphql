query StopPlacesHaveDepartures($ids: [String]) {
    stopPlaces(ids: $ids) {
        id
        quays {
            lines {
                transportMode
                transportSubmode
            }
        }
    }
}
