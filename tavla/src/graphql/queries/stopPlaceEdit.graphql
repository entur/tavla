query stopPlaceEdit($placeId: String!) {
    stopPlace(id: $placeId) {
        name
        quays {
            id
            publicCode
            name
            description
            stopPlace {
                transportMode
            }
            ...lines
        }
    }
}
