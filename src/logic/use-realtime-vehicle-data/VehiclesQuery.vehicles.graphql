#import "./VehicleFragment.vehicles.graphql"

query VehiclesQuery(
    $boundingBox: BoundingBox
) @api(name: vehicles) {
    vehicles(
        boundingBox: $boundingBox
    ) {
        ...VehicleFragment
    }
}
