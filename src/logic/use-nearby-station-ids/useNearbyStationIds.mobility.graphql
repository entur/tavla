query NearbyStationIds(
    $latitude: Float!
    $longitude: Float!
    $range: Int!
    $formFactor: [FormFactor]
) @api(name: mobility) {
    stations(
        lat: $latitude
        lon: $longitude
        range: $range
        availableFormFactors: $formFactor
    ) {
        id
    }
}