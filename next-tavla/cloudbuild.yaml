steps:
    - name: 'gcr.io/cloud-builds/docker'
      args:
          [
              'build',
              '--build-arg',
              'ENV=${_ENV}',
              '--build-arg',
              'ENABLED_FEATURES=${_ENABLED_FEATURES}',
              '-t',
              'eu.gcr.io/$PROJECT_ID/$PROJECT_ID:$SHORT_SHA',
              '.',
          ]
images: ['eu.gcr.io/$PROJECT_ID/$PROJECT_ID:$SHORT_SHA']
