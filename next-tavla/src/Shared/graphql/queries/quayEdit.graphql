query quayEdit($placeId: String!) {
    stopPlace(id: $placeId) {
        quays(filterByInUse: true) {
            id
            publicCode
            description
            stopPlace {
                transportMode
            }
            journeyPatterns {
                directionType
            }
        }
    }
}
