name: Linting and Formatting
on:
  push:
    branches-ignore:
      - master
    paths-ignore:
      - "tavla/*"
jobs:
  linting_and_formatting:
    name: Verifies linting and formatting
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No linting or formatting required"'
